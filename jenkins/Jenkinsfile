#!/usr/bin/env groovy

pipelina{
    agent none
    stages{
        stage{
            agent{
                docker{ image 'maven'}
            }
            steps{
                sh 'mvn package -Dmaven.test.skip=true'
            }
        }
    }
}